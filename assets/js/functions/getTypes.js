// const Type = {
//   constructor(type, url){
//     this.type = type;
//     this.url = url;
//   }
// }

function getTypes(types) {
  // let types = [];
  types.forEach(type => {
    // types.push(new Type(type));
  });
}